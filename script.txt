Fonctionnalite 1 : Ajout de fond dans l'établissement financier (EF) (Tina)
- CRUD  
- Table : 
    EtablissementFinancier
    TypeOperation (depot)
    OperationFonds


Fonctionnalite 2 : création type de prêt avec différent taux (Tina)
- CRUD
- Table :
    TypePret


Fonctionnalite 3 : gestion de prêt pour les clients (Joe)
- CRUD pret 
- Table :
    pret (insert)
    StatutPret 
    Remboursement (insert automatique avec statut impaye)
    
    
Remboursement : (remboursement)
- Table :
    StatutRemboursement
    Remboursement
Update montant restant (id_pret)


Créer un tableau pour afficher les intêrets gagnés pour mois pour l'EF. 
Mettre un filtre (mois et année début ) et mois/année fin

Créer un graphique à partir du tableau 


<div>
                        <button class="btn-pdf" onclick="genererPDF(${pret.id_pret}, event)">
                          <i class="fas fa-file-pdf"></i> PDF
                        </button>
                    </div>



 // Nouvelle fonction pour générer PDF
      // function genererPDF(pretId, event) {
      //     event.stopPropagation(); // Empêche l'ouverture de la modal
      //     window.open(`${apiBase}/prets/${pretId}/pdf`, '_blank');
      // }
      function genererPDF(pretId, event) {
        event.stopPropagation();
        
        // Solution temporaire - enlevez '/ws' de l'URL
        const baseUrl = apiBase.replace('/ws', '');
        window.open(`${baseUrl}/prets/${pretId}/pdf`, '_blank');
      }